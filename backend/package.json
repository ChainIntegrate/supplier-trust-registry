{
  "name": "supplier-trust-registry-backend",
  "version": "1.0.0",
  "description": "Proxy di autenticazione (SIWE-style su Universal Profile) e upload IPFS/Pinata per Supplier Trust Registry. Non gestisce mai cifratura ne' PIN utente: riceve solo byte gia' pronti (cifrati o pubblici) e li inoltra a Pinata.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.4",
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
